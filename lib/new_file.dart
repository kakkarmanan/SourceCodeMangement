import 'package:flutter/material.dart';

class new_file extends StatelessWidget {
  const new_file({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(),
    );
  }
}
